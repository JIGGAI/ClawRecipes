# Optional Basic Auth for ClawRecipes Kitchen
# Copy to .env and set values to enable login.
# KITCHEN_AUTH_ENABLED=true
# KITCHEN_AUTH_USER=admin
# KITCHEN_AUTH_PASSWORD=your-secure-password
# Or read password from file (takes precedence over KITCHEN_AUTH_PASSWORD; recommended for Docker/secrets):
# KITCHEN_AUTH_PASSWORD_FILE=/run/secrets/kitchen-password
# Restrict password file to mode 0600 when possible.
